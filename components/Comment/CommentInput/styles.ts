import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 5,
        flexDirection: 'row',
        width: '100%'
    },
    inputContainer: {
        marginTop: 5
    }
})

export default styles